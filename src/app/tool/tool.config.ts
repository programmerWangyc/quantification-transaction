export enum LogTypes {
    BUY,
    SALE,
    RETRACT,
    ERROR,
    PROFIT,
    MESSAGE,
    RESTART,
}

export enum SemanticsLog {
    runningLog = 'runningLog',
    profitLog = 'profitLog',
    strategyLog = 'strategyLog',
}
