export enum Language {
    JavaScript,
    Python,
    'C++',
}

export enum OpStrategyTokenTypeAdapter {
   GET,
   UPDATE,
   DELETE,
   ADD,
}

export enum StrategyVisibilityType {
    publicOrRentable = -2,
    visibleMySelf,
}
